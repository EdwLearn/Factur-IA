# FacturIA API — dependencias consolidadas para entorno facturIA (Python 3.11)
# Generado desde pyproject.toml + requirements.txt raíz

# ── Framework ────────────────────────────────────────────────────────────────
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6

# ── Validación / configuración ───────────────────────────────────────────────
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Base de datos ────────────────────────────────────────────────────────────
sqlalchemy==1.4.49
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
databases==0.8.0

# ── AWS ──────────────────────────────────────────────────────────────────────
boto3==1.34.34
botocore==1.34.34

# ── Documentos / PDF ─────────────────────────────────────────────────────────
pdfplumber==0.11.0
PyPDF2==3.0.1
img2pdf==0.5.1
reportlab==4.0.9
# opencv-python tiene problemas en WSL2 sin display — se usa headless
opencv-python-headless==4.9.0.80
pillow==10.2.0

# ── XML (DIAN UBL 2.1) ───────────────────────────────────────────────────────
lxml>=4.9.0

# ── ML / NLP ─────────────────────────────────────────────────────────────────
# torch es muy pesado — instalar CPU-only para dev
torch==2.1.2+cpu
transformers>=4.30.0
scikit-learn==1.4.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# ── Autenticación / seguridad ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.5

# ── Cache / cola ─────────────────────────────────────────────────────────────
redis==5.0.1

# ── HTTP / async ─────────────────────────────────────────────────────────────
httpx==0.27.0
requests==2.31.0

# ── Utilidades ───────────────────────────────────────────────────────────────
python-dotenv==1.0.0
structlog==23.2.0
tenacity==8.2.3
backoff==2.2.1
jinja2==3.1.3
numpy==1.26.3
pandas==2.2.0
